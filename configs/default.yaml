---
num_generations: 5000
load_population: ~
CCEA:
  sub_population_size: 15
  num_parents: 5
  mutation_rate: 0.15
  mutation_probability: 0.15
  nn_hidden:
    - 14
    - 14
  num_workers: 8
  use_difference_evaluations: True
  num_evaluations: 1
  config:
    BoidsEnv:
      max_steps: !eval 15*2
      render_mode: none
      init_seed: 0
      config:
        map_dimensions:
          x: 100
          y: 100
        StateBounds:
          min_velocity: 0
          max_velocity: 20
          max_acceleration: 10
          max_angular_velocity: !eval 3.14159 * 0.5
          num_leaders: 3
          num_followers: 10
        BoidSpawner:
          spawn_rule: Circle
          radius_fraction: !eval 0.08
          velocity_fraction: !eval 0
          fix_all: True
        BoidsColony:
          radius_repulsion: 2
          radius_orientation: 3
          radius_attraction: 5
          repulsion_multiplier: !eval 3*0.3
          orientation_multiplier: !eval 1*0.3
          attraction_multiplier: !eval 1*0.3
          wall_avoidance_multiplier: !eval 1*0.3
          dt: !eval 1/2
        POISpawner:
          poi_spawn_rule: FixedConcentricCircles
          num_pois: ~
          bound_fraction: !eval 1/2
          inner_circle_bound_fraction: !eval 1/5
          outer_circle_bound_fraction: !eval 2/5
          fix_poi_spawns: False
          num_pois_concentric:
            - 3
            - 15
          concentric_radii_fraction:
            - 0.2
            - 0.5
        POIColony:
          observation_radius: 5
          coupling: 5
        ObservationManager:
          observation_rule: Individual
          num_poi_bins: 4
          num_swarm_bins: 8
          poi_sensor_type: InverseDistance
          swarm_sensor_type: InverseDistance
        Renderer:
          pixels_per_unit: 10
          leader_colors:
            - [250, 120, 0]
            - [120, 0, 250]
            - [250,250,0]
            - [0,200,0]
            - [120,0,120]
            - [66,245,242]
